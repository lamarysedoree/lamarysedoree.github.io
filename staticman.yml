# staticman.yml

name: Staticman Comments

on:
  push:
    branches:
      - main  # ou master

acceptance:
  headless: true

permalink: /comments/{slug}

publish:
  branch: main
  path: "_comments/{slug}.json"

comments:
  allowedFields: [name, email, message]
