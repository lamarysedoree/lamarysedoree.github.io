name: Staticman Comments

on:
  push:
    branches:
      - main  # ou votre branche principale

acceptance:
  headless: true

permalink: /bavarois/  # URL de votre page ou article

publish:
  branch: main
  path: "_comments/bavarois.json"

comments:
  allowedFields: [name, email, message]
